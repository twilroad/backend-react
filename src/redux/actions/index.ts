import * as TodoActions from './todo';
import * as GlobalActions from './global';

export const ActionCreators = Object.assign({}, TodoActions);

export const GlobalCreators = Object.assign({}, GlobalActions);
